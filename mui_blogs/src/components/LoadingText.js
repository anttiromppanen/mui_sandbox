import React from 'react';
import { Typography } from '@mui/material';

function LoadingText() {
  return <Typography variant="h5" textAlign="center" mt={8}>Loading...</Typography>;
}

export default LoadingText;
